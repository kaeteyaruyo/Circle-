const config = {
    projectRoot: '',
    protocol: 'http',
    host: '',
    port: '',
    cookieSecret: '',
    staticUrl: () => `${ config.protocol }://${ config.host }:${ config.port }`
}

module.exports = config;